node_modules/
dist/
dist-ssr/
*.local
dev.log
dev-run.log
.zcode/
.vite/
.edge-tmp/
*.png.tmp
shot-*.png

# rust / tauri
src-tauri/target/
src-tauri/gen/schemas/

# editors
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.njsproj
*.sln
*.sw?
*.tsbuildinfo

# Local scratch: throwaway scripts & screenshots
.scratch/

# Third-party icon references downloaded for visual comparison only —
# never commit or ship these (see AGENTS.md "代码来源边界").
tmp-refs/

# Icon exploration output (contact sheets, per-concept renders). Regenerate any
# of it with scripts/icon-lab.mjs; only the master and the snapshots are kept.
src-tauri/icons/candidates/

# ── Publication boundary (2026-09-12) ─────────────────────────────────────
# This copy is the PUBLIC tree: code, build config and tests only. All local
# planning, review, acceptance and ops notes live in the workspace's
# internal/ directory (D:\AI项目\dsh-phl\internal) and must never be tracked
# here — the 2026-09-12 reconciliation fast-forwarded this copy to the
# workspace line and published them silently. The single rule below mirrors
# BOUNDARY in scripts/check-public-boundary.mjs; change both together.
internal/
# Root compat pointer to AGENTS.md — local workspace only.
CLAUDE.md
