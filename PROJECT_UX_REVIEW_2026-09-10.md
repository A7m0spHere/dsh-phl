# PHL 用户体验优化报告（Agent Team · 2026-09-10）

> 方式：3 条互不重叠的 UX 审查线（信息架构 / 高频流程 / 视觉与可访问性）+ Lead 在浏览器 Mock 模式（npm run dev，localhost:5180）的实测（截图、DOM 度量、核心旅程走查）。
> 本轮只谈**用户感受到什么**；代码正确性与重构类问题见 PROJECT_REVIEW_2026-09-10_agent-team.md。
> 明细：.scratch/review/ux-ia.md、ux-flows.md、ux-craft.md；实测截图：.scratch/ux-shot/*.png（Lead 本人拍摄，下文标注）。
> 结论：**没有 P0** —— 核心旅程（创建 → 启动 → 用 WebUI → 维护 → 卸载）都能走通；问题集中在「状态说不清、失败找不回、承诺没兑现」三类。

---

## 一、最值得先做的 13 条（P1）

### A. 启动与创建（每一天都会遇到）

**1. 启动成功必然开一个新 WebUI 窗口并抢焦点，而设置里没有任何开关**
- 场景：用户点「启动」只想让实例跑起来（接着配另一个实例、或让它后台跑），却被弹出的窗口打断。
- 现状：src/stores/instanceStore.ts:322 无条件执行 if (isDesktop) void get().openWebUi(id)；settingsStore 里 0 处 webui 相关偏好（已复核）。连开多个实例 = 连开多个窗口，系统学不到偏好。
- 建议：设置 →「实例」增加 启动后：自动打开 WebUI / 只在我点「打开」时 / 每次询问（默认保持自动，不破坏「即启即用」的承诺）；更轻的方案是建窗但不抢焦点；启动成功的 toast 上可加「以后不再自动打开」。
- 成本/收益：S–M / 影响每一次桌面启动。

**2. 刚创建、组件还在后台下载时点「启动」，会被报「版本还没有安装到本机，请去版本页安装」——与正在发生的事直接矛盾**
- 场景：创建向导已明说「创建后将自动下载 DSH x / Node 22」（CreateInstancePage.tsx:282-286），用户落到详情页立刻点「启动」。
- 现状：CreateInstancePage.tsx:220-229 在后台链式下载并提示「正在后台下载，完成后即可启动实例」；而 tauriLaunches.ts:47-61 对**任何非 installed 状态**（含 queued/downloading/extracting/verifying/installing-deps）都抛「DSH 版本未安装 / 前往版本页安装」。实例卡片与详情页也没有「准备中」状态，启动按钮照常高亮可点。
- **Lead 实测复现**（截图 21-dark-detail-longname.png、23-small-window.png）：创建后立即启动 → 报「启动失败 / DSH 版本未安装」；几秒后同一实例弹出「DSH 0.1.3-alpha.1 安装完成」。用户先被告知"没装，去装"，再被告知"装好了"。
- 建议：把未就绪分三态：可用（现有文案）/ 失败（原因 + 重试下载）/ **忙碌**（「正在下载 DSH x（42%），就绪后即可启动」+ 查看进度）；忙碌时详情页主按钮变「等待环境就绪（下载 42%）」，卡片加「准备中」标识。
- 成本/收益：S–M / 直接决定新用户对"这软件靠不靠得住"的第一印象。

**3. 失败信息没有可回看的地方**
- 场景：用户在忙别的（或同时开 3 个下载），一条失败提示闪过；之后想找原因，实例页、任务中心、设置页都找不到。
- 现状（已复核）：uiStore.ts:420 的 toasts.slice(-3) 把栈硬限在 4 条 —— 第 5 条会把**唯一那条错误**挤掉；:416 错误 toast 7 秒自动消失；点「忽略」后 instanceStore.ts:758-761 连 hint 一起永久消失；启动/停止不在 Rust 的 guarded 任务里，任务中心不兜底；taskStore.recentFailures()（注释写着是重试入口）**零消费**。
- 建议：① 错误 toast 不参与限额淘汰，duration 0 常驻直到手动关闭；② 实例上保留「上次启动失败 12:03 · 原因 · 重试」，下次成功或显式清除才移除；③ 任务中心顶部接一个「最近失败（可重试）」区。
- 成本/收益：S–M / 把"失败不可追"变成"失败可复现"。

**4. 任务中心的行标题是内部 id，还被「保存实例」刷满**
- 场景：下载版本 / 克隆 / 快照 / 删除时打开「任务」面板，或回顾刚才那次失败。
- 现状：行标题优先用 Rust 拼的 task.label，而它是内部 id（instances/mod.rs:312 的「删除实例 {id}」、:279、:475、snapshot.rs:114/302；已复核）；实例 id 是 slug + 4 位随机，tauriInstances.ts:27-30 还会把中文名整段抹掉（探针实测：我的开发环境 → instance-p3qa）。于是界面别处显示"我的开发环境"，任务行却是「删除实例 instance-p3qa」。另外每次改名/点★都会注册一条 instance-save，而面板只列 12 行（TaskCenter.tsx:37）→ 真失败被挤走。
- 建议：label 用显示名；把 instance-save / plugin-toggle 这类瞬时空任务折叠或过滤；把 recentFailures 渲染成顶部失败区。
- 成本/收益：M / 决定"出问题能不能自己查到原因"。

### B. 高频维护动作

**5. 实例运行中「创建快照」「回滚」照常可点：用户走完确认框才被告知「先停止实例」**
- 现状（已复核）：InstanceDetailPage.tsx:838 的 disabled 只看 !!snapshotOp，而 store 守卫在 instanceStore.ts:436/497 才拒绝。回滚更绕：确认框里写着"当前 dsh-home 里的插件与配置会被整体替换"，用户确认后才收到"先停止实例，再还原快照"，什么都没发生。
- 建议：最小改法 = 运行中禁用 + tooltip「实例运行中——先停止才能创建或回滚快照」；更好 = 二段式「停止并创建快照 / 停止并回滚」（确认框里写明会中断 DSH 里正在进行的对话）。
- 成本/收益：S（禁用+文案）/ M（停止并执行）/ 影响每次"改环境前存档"。

**6. 一个实例同步中时，点另一个实例的「同步」是完全无声的死点击**
- 现状（已复核）：apiConfigStore.ts:297 的 if (syncing) return null 静默返回，而按钮按**自己这一行**是否同步来禁用（InstanceBindingRow.tsx:22/139）→ 别的行可点；调用方拿到 null 后没有 else 分支。有本地改动时用户还会先确认「覆盖本地修改」，然后什么都没发生。
- 建议：按全局 syncing 禁用并显示「另一个实例正在同步…」，或把 syncing 改成按实例的集合并行同步；无论如何返回 null 必须给一条 toast。
- 成本/收益：S / 多实例用户的必经路径，属于"看着能用其实没做"。

**7. 删除实例的确认框不提「快照会一并删除」**
- 现状：useInstanceActions.tsx:97-105 的文案只列 DSH_HOME / 插件 / Profile / workspace，而快照就存在实例目录内（snapshot.rs:52 的 dir.join("snapshots")），delete_instance 整树删除（对应代码审查 2.3）→ 用户唯一的回滚凭据被静默删掉；详情页明明已有快照数量与体积。
- 建议：确认框补「含 N 个快照（合计 X），会一并删除」+ 建议先导出整合包；进阶可用仓库已有的"先改名再删"原语做 7 天可恢复窗口。

### C. 首次接触与信息架构

**8. 新手引导与现在的产品对不上三处**
- 现状：引导标题写「五个页面」但实际 6 个标签；清单只列 4 个（GuideOverlay.tsx:234/120-140 vs TitleBar.tsx:35-42）；「模型与 API」和「设置」整份引导一字未提；末步「点底部那条栏上的启动」指向不存在的控件（启动栏只在实例列表页 InstancesPage.tsx:399，而创建后落地在详情页）。
- 建议：把引导改成按用户旅程讲（建实例 → 装版本/Runtime → 配 API → 启动 → 打开 WebUI），末步只做指路而不硬编码控件位置。

**9. 首次创建实例时 API 配置全程隐形 → 得到一个没有可用模型的 DSH**
- 现状：向导默认 apiInheritance 为 default（wizardStore.ts:15-17）而全局库是空的，API 控件折叠在「更多设置」里、右侧摘要也不列该行（sections.tsx:594-604/795-817）。用户看到「配置就绪」。
- 建议：全局库为空时在向导显著位置提示「还没有 API 配置，实例将无法对话」+「现在去配置 / 稍后再说」，创建落地后也提示一次。

**10. 版本目录拉取失败被吞掉 → 界面把「零数据」说成「换筛选条件」**
- 现状：tauriVersions.ts:32-42 吞错，catalogStore.ts:56-59 那条「版本目录加载失败+重试」toast 是死代码；版本页 VersionsPage.tsx:506-507 与创建向导 sections.tsx:257-275（Runtime 区只有空网格，连空态文案都没有）都在误导；点「同步更新」还回「已是最新版本」。
- 建议：区分"目录为空 / 加载失败"与"筛选后为空"；失败态给原因 + 重试；Runtime 区补同样的空态。

**11. 同一概念两个名字**
- 现状：实例详情菜单里「导出 Bundle」和「导出整合包」并排且无差异说明（useInstanceActions.tsx:260/263），两者承诺几乎相同；实例页主按钮是「安装整合包」，而「导入 Bundle」藏在「更多导入方式」里。
- 建议：统一成一个用户词（建议"整合包 / .phlpack"），旧名作为括号里的兼容说法；或在菜单里加一行差异说明。

**12. 插件作用实例跨页静默换人**
- 现状：从实例 B 详情页点顶部「插件」，pluginScope 为空 → 回退 instances[0] 并写回固化（viewStore.ts:122-124、PluginsPage.tsx:61-66）→ 用户以为在看 B，其实在看 A。
- 建议：复用已有的 setFocus 模式（InstanceDetailPage.tsx:136-140），跳转前写入作用实例。

### D. 视觉与可访问性

**13. 浅色主题下状态色文字对比度不达标（实测 2.55:1 ~ 3.21:1，AA 要求 4.5:1）**
- 现状（按 token 实算）：StatusPill「停止中」2.68:1、「运行中」3.21:1；Badge warn（「未安装 / 版本不匹配 / 兼容性未知」）2.55:1；Notice warn 标题 2.63:1；字号只有 10px。深色主题同一批组合全部 ≥4.4:1。
- 关键点：index.css:24-27 只声明 ink/muted/faint 量过对比度，**语义色没量**；而"未安装"正是启动失败的根因提示。
- 建议：补 --c-ok-ink / --c-warn-ink / --c-danger-ink 文字专用色（像 --c-accent-ink 那样），圆点与进度条仍用亮色；并把"语义色对比度"写进既有注释块防止再漂移。
- 成本/收益：S / 全应用所有状态标签同时受益。

---

## 二、Lead 实测补充（浏览器 Mock 模式，截图证据）

**14. 长实例名会把详情页标题撑破，压住状态徽标与操作按钮**
- 证据：暗色截图 .scratch/ux-shot/21-dark-detail-longname.png 与 1024×680 截图 23-small-window.png：32 字中文名在标题行溢出到右侧，与「已停止」徽标和「启动」按钮区域重叠；1024 宽时直接折成两行压住整行。
- 根因（已复核）：InstanceDetailPage.tsx:275 的 div.flex.items-center.gap-3 缺 min-w-0，flex 约束链断在这里，:285 的 truncate 拿不到受限宽度；PageShell 只在 Page.tsx:47 的 wrapper 上给了 min-w-0。
- 建议：给 :275 及同层 flex 容器补 min-w-0，并给卡片/启动栏的长名补 title 提示。成本 S。

**15. toast 居中堆叠会盖住内容区顶部，窄窗口下正好压住实例标题与主操作**
- 证据：23-small-window.png 里 3 条 toast 叠在内容区顶部，遮住实例标题行、「运行环境」卡片首行与部分操作区；Toaster.tsx:108 是 fixed left-1/2 top-[calc(var(--titlebar-h)+10px)] 居中、每条 312px、向下堆叠。
- 建议：① 错误常驻、成功/信息自动消失（配合第 3 条）；② 改右上/右下角锚定，或超过 2 条折叠成「N 条通知」；③ 至少不要遮住当前页面的主操作区。成本 S–M。

**16. 表单未填必填项时，主按钮外观完全可用**
- 证据：18-blank-click.png + DOM 度量：名称为空时「创建实例」按钮 disabled=false、opacity=1、cursor=pointer、无 aria-disabled；点击后才出现内联错误（输入框变红 +「请填写实例名称」+ 右侧「未完成」徽标 + 底部「还需要选择：名称」）。
- 评价：校验本身做得不错（内联、明确），但"看起来能点、点了才知道缺什么"多了无谓一步。
- 建议：保持可点也行，但点击后应**聚焦并滚动到第一个错误项**；或直接 disabled + tooltip 说明缺什么。成本 S。

**17. 版本页对「未安装版本」缺少提示：选了它创建实例，必然得到一个启动失败的实例**
- 证据：04-versions.png、10-create-blank.png + 实测：0.1.3-alpha.1 行显示「npm 未收录 · 在 GitHub 查看 · 让 agent 构建」、大小 0 B；创建向导默认选中它，右侧摘要是「DSH 版本 0.1.3-alpha.1」，创建后启动即失败（见第 2 条）。
- 建议：① 向导里对未安装版本显式二选一「创建后自动下载（约 47 MB）」/「先去安装」，把"自动补齐"从隐藏行为变成明示承诺；②「让 agent 构建」这类黑话要有一句解释（构建什么、多久、谁执行），否则普通用户不会点。

**实测的正面结论**：125% / 150% 缩放下 scrollWidth == innerWidth（无横向滚动条，截图 22-zoom-125.png / 22-zoom-150.png）；暗色主题观感与浅色一致（20-dark-versions.png）；1024×680 下主导航标签仍可见（860px 以下才隐藏，见 P2 表）。

---

## 三、其余值得排期的（P2 / P3 精选）

| 优先级 | 问题 | 位置 | 建议 |
| --- | --- | --- | --- |
| P2 | 「排队中」只在当前行可见，任务中心完全看不到排队，用户不知道为什么在等 | transferCoordinator.ts:19-65、catalogVersionActions.ts:74-75、TaskCenter.tsx:64-68 | 暴露只读快照（运行中 + 等待队列位置）并渲染进任务中心；文案统一为「排队中，等待传输槽位（上限 N），前面还有 M 个」 |
| P2 | 安装整合包时切页 = 静默取消（对照：导出整合包切页会提示「已取消导出」） | PackInstallPage.tsx:67、packStore.ts:163 | 切页时保留任务或先确认；至少取消/失败要有 toast |
| P2 | 运行中崩溃只说"日志在 logs/ 下"却没有入口 | instanceStore.ts:169-177、InstanceDetailPage.tsx:645-700 | toast 给「打开日志目录」动作（启动阶段失败已有 Notice 展示日志尾，参照它） |
| P2 | 加载失败后页面永久骨架屏（5 个页面同一模式），重试入口 7 秒后随 toast 消失 | instanceStore.ts:194-211、InstancesPage.tsx:347-352 等 | 在 ui/Feedback.tsx 增加带重试的 ErrorState，页面态改 loading/error/ready 三态 |
| P2 | 关闭动效设置无效：呼吸光晕、骨架 shimmer 是 CSS 关键帧，不受 useMotion() 控制；同时 MotionConfig 传 never 等于显式忽略系统 prefers-reduced-motion | StatusPill.tsx:42、Feedback.tsx:102、App.tsx:252 | data-motion 属性 + 三行 CSS 关掉关键帧；新增「跟随系统」档 |
| P2 | 自绘标题栏拖拽热区比看起来小：抓 Logo 或「PHL」字样拖不动、双击不最大化（Tauri 2.11.5 对裸 data-tauri-drag-region 只认点击目标本身） | TitleBar.tsx:81/85 | 改成 data-tauri-drag-region="deep"，可交互元素会被自动豁免 |
| P2 | 窗口 ≤860px 时 6 个页签只剩图标，无 aria-label、无 tooltip、无 aria-current | index.css:197-201、TitleBar.tsx:131-157 | 加 aria-label + Tooltip（一行/一处） |
| P2 | 报错文本基本选不中、右键被全局禁用 → 用户没法复制报错去搜索/求助 | index.css:229-244、App.tsx:199-209 | 把 Notice / 错误态 / toast / DataRow 的值容器纳入 .selectable 与右键白名单 |
| P2 | 「复制路径 / 地址」按钮悬停才出现，键盘 Tab 到的是隐形按钮 | Card.tsx:162-166 | 容器加 group-focus-within/row:opacity-100（或静态 40% 透明） |
| P2 | 命令面板覆盖不全（缺 模型与 API / 接入本机 DSH / 安装整合包 / 打开 WebUI / 创建快照），空查询时反而过滤掉启动与停止；版本查不到显示「DSH undefined」 | QuickSwitcher.tsx:99-142/149/70-75 | 补齐命令 + 空查询不过滤 + 复用列表页的 id 兜底 |
| P2 | 已算好的 hint（修复建议）在十余处只取 message，Toast 模型根本没有 hint 字段；Toaster 无 aria-live | errorCodes.ts:34-43、uiStore.ts:172-181 | Toast 增加 hint 并渲染；容器加 aria-live="polite" |
| P2 | 启动失败时卡片与启动栏只剩被截断的 error.title 和盲「重试」，「去安装 DSH/Runtime」只在详情页 | InstanceCard.tsx:144-154/59-63、LaunchDock.tsx:126 | 卡片错误行加 detail+hint 的 Tooltip，失败态主按钮按根因变成「安装 DSH x」/「安装 Runtime」 |
| P3 | 新建实例草稿在关窗时无提示直接丢失 | App.tsx:145-195、wizardStore.ts:26-32 | 复用现有 ui.confirm，标题改为「退出 PHL？未完成的实例草稿会丢失」 |
| P3 | 圆角 token 与 AGENTS.md 不一致（文档 10/8/4px，实际 8/6/3px）；UIScale 的 125 无入口 | tailwind.config.js:40-47、uiStore.ts:218 | 二者对齐其一；删掉不可达的值或补入口 |
| P3 | 卡片长名截断无 title（dsh-prod-1 / dsh-prod-2 无法区分） | InstanceCard.tsx:103 | 补 title={instance.name} |

---

## 四、已经做得好、建议保持的（避免回退）

- **失败解释的标准动作**：启动失败的 Notice 给「原因 + 一句修复建议 + 可执行按钮（去安装 DSH / 忽略）」（InstanceDetailPage.tsx:383-412）——这是全应用最好的错误体验，其他失败路径应向它对齐。
- **破坏性确认**：删除实例要求逐字输入实例名；覆盖同步会列出代价；快照回滚说明会整体替换（问题只在"该禁用时没禁用"，不在文案）。
- **任务中心常驻 + 页面隐藏时暂停轮询 + 切回立即刷新**。
- **列表页三态区分**：实例页已区分「还没有实例」与「没有匹配的实例」并各给对应动作（版本页与 Runtime 区应照做）。
- **桌面能力边界诚实**：浏览器模式明确标注「浏览器模拟模式」，接入与整合包页直接说明「桌面端功能」。
- **缩放与主题**：125% / 150% 缩放下无横向滚动，暗色主题观感一致（截图 20-dark-versions.png、22-zoom-125.png）。
- **快捷键目录与实际绑定一致**（切片 A 逐条核对过），缺的只是「按 ? 查看快捷键」的发现入口。

---

## 五、建议的实施顺序（按投入产出）

1. **一天能做完、体感最强**：第 13 条（语义色文字对比度）、第 5 条（快照按钮禁用 / 二段式）、第 6 条（同步静默返回）、第 3 条前半（错误 toast 常驻且不被挤掉）、第 14/15/16 条（长名 min-w-0、toast 位置、按钮态）——全部 S 成本。
2. **一个迭代**：第 2 条（busy/failed 三态 +「准备中」状态）、第 4 条（任务中心显示名 + 降噪 + 最近失败区）、第 12 条（插件作用实例）、第 10 条（目录加载失败态）、第 8/9 条（引导与首次 API 配置）。
3. **需要一点设计**：第 1 条（启动后是否自动开窗的偏好）、第 7 条（删除实例的完整后果说明与可恢复窗口）、第 17 条（未安装版本的显式承诺）、第 11 条（导出术语统一）。
4. **随体验基线一起做**：P2 表里的队列可见性、ErrorState、动效开关、拖拽热区、窄窗口导航、可复制文本、卡片失败态。

> 验收建议：npm run typecheck + npx vitest run 全绿，再用 npm run dev 在浏览器 Mock 模式走一遍「首次启动 → 创建 → 启动失败 → 安装版本 → 启动成功」「运行中创建快照」「多实例同步」三个场景；桌面独有项（自动开窗与焦点、标题栏拖拽、托盘、关闭确认）需真机补测。